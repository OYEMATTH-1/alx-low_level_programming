#include "main.h"

/**
 * _strlen - a function that returns the length of a string
 * @s: char input
 * Return: the length of input string
 */
int _strlen(char *s)
{
	int = 1, sum = 0;
	char length = s[0];

	while (length != '\0')
	{
		sum++;
		length = s[i++];
	}
	return (sum);
}
