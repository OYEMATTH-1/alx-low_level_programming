#include <stdio.h>

/**
 * main - Prints all the numbers of base i6 in lowercase
 * followed by a new line
 * Return: Always o (Success)
 */

int main(void)
{
	char ch = 'z';

	while (ch >= 'a')

		int '1' = '0';

	while (i <= '9')
	{
		putchar(ch);
		ch--;
	}

	{
		putchar(i)
			i++
	}

	putchar('\n');
	return (0);
}
